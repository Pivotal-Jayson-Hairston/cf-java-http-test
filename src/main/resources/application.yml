spring:
  profiles:
    active: java-client

security:
  basic:
    enabled: false

management:
  security:
    enabled: false
    
cf:
  debug: false
  verbose: true

connection:
  intervalMs: 300000
